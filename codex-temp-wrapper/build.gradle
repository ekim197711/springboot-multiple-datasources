
tasks.withType(Test).configureEach {
    useJUnitPlatform()
}

dependencies {
    runtimeOnly "jakarta.xml.bind:jakarta.xml.bind-api:4.0.5"
    runtimeOnly "javax.xml.bind:jaxb-api:2.3.1"
}// Temporary build used only to generate Gradle wrapper files.
